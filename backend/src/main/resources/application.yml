spring:
  application:
    name: backend

  datasource:
    url: jdbc:h2:file:./db_dev
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
